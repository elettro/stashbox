name: One-Time Clean VEC Render Settings Branch (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled after successful branch cleanup.'

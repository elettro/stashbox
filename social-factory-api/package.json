{
  "name": "stashbox-social-api",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Isolated Stashbox Social Factory API Lambda.",
  "main": "index.mjs",
  "scripts": {
    "check": "node --check index.mjs && node --check youtube-oauth.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
